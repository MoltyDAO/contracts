services:
  foundry:
    build: .
    container_name: moltydao-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    env_file:
      - .env
    ports:
      - "8545:8545"
      - "3000:3000"
    stdin_open: true
    tty: true
